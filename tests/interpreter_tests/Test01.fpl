fun sum(a, b) {
    return a + b
}

println(sum(1, 2))

var a = 10
var b = 20
if (a > b) {
    println(1)
} else {
    println(0)
}

fun fib(n) {
    if (n <= 1) {
        return 1
    }
    return fib(n - 1) + fib(n - 2)
}

var i = 1
while (i <= 5) {
    println(i, fib(i))
    i = i + 1
}

fun foo(n) {
    fun bar(m) {
        return m + n
    }

    return bar(1)
}

println(foo(41))

b = a + (a + a) * a % a / a     // b = 10
println(a)
println(b)
println(b < a)
println(b > a)
println(b || a)
println(b <= a)
println(b >= a)
println(b && a)
println(b == a)
println(b != a)
println(0 || 1)
println(1 && 0)

if (a || b || 1) {
    fun f(n, m) {
    }
    f(a, b)
} else {
    while (a) {
        while(b) {
            println(123)
        }
    }
}