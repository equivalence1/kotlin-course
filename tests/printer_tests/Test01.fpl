var a = 10
var b = 20
if (a > b) {
    println(1)
} else {
    println(0)
}

fun fib(n) {
    if (n <= 1) {
        return 1
    }
    return fib(n - 1) + fib(n - 2)
}

var i = 1
while (i <= 5) {
    println(i, fib(i))
    i = i + 1
}

fun foo(n) {
    fun bar(m) {
        return m + n
    }

    return bar(1)
}

println(foo(41))

fun foo() {
    fun foo() {
    }
}

foo()

var b = a + (a + a) * a % a / a
println(b < a)
println(b > a)
println(b || a)
println(b <= a)
println(b >= a)
println(b && a)
println(b == a)
println(b != a)

if (a || b || 1) {
    fun f(n, m) {
    }
    f(a, b)
} else {
    while (a) {
        while(b) {
            println(123.123)
        }
    }
}